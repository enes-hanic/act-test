# This is a basic workflow to help you get started with Actions

name: Create staging branches

# Controls when the action will run. 
on: create
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  sync-branches:
    runs-on: ubuntu-latest
    name: Create branches
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set output
        id: vars
        run: echo ::set-output name=short_ref::${GITHUB_REF#refs/*/}
      - name: Check output
        run: echo ${{ github.event.ref }}
      - name: Check output 2
        if: startsWith(github.event.ref, 'site9-') && endsWith(github.event.ref, '-newcart') || endsWith(github.event.ref, '-newtheme')
        run: echo ${{ steps.check.outputs.name }}
      - name: Set up Node
        uses: actions/setup-node@v1
        with:
          node-version: 12
#       - name: Create branch
#         uses: peterjgrainger/action-create-branch@v2.0.1
#         if: ${{ steps.check.outputs.name }} == 'true'
#         env:
#           GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
#         with:
#           branch: staging/${{steps.vars.outputs.short_ref}}
